apiVersion: v1
kind: Service

metadata:
  name: glance

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: glance

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP